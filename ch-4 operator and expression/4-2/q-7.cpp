#include<iostream>
using namespace std;
main(){
    // (x+y+z)^2
    float x,y,z;
    cout << "Enter the value of X := ";
    cin >> x;
    cout << "Enter the value of Y := ";
    cin >> y;
    cout << "Enter the value of Z := ";
    cin >> z;
    cout << "(x+y+z)^2 = " << (x+y+z)*(x+y+z);
}