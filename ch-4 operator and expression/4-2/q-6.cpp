#include<iostream>
using namespace std;
main(){
    // (x-y)^3
    float x,y;
    cout << "Enter the value of X := ";
    cin >> x;
    cout << "Enter the value of X := ";
    cin >> x;
    cout << "(x-y)^3 = " << (x-y)*(x-y)*(x-y);
}